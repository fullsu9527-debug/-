# 高中物理例题整合与变式工作台 — 项目说明（CLAUDE.md）

> 放在仓库根目录。Claude Code 每次进项目会先读这份文件。改任何东西前先读完"绝对不能改"和"代码现状"两节。

## 这是什么
单页 Web 应用，配套硕士论文《多版本高中物理教材例题资源的整合与拓展策略研究——以必修1为例》。面向一线高中物理教师，用于必修一的备课、组题、改题、出讲义和课堂投屏。当前是单文件 HTML（约 8 MB，含 base64 图片），发布在 GitHub Pages。

## 本轮目标
为教师真实课堂使用打磨。判断标准始终是：**教师能否几步内拿到一份能直接上课的题**。研究展示、六维编码术语一律折叠后置，不挡在备课路径上（分层不删除）。

## 绝对不能改的（最高优先级）
- 任何例题的编码数字、维度赋值、题目内容都不许改。权威源是 `六版本必修一例题六维度全样本编码总表.xlsx`，应用只负责展示。
- 不要"纠正"这几道（看着像层级Ⅱ，其实是Ⅰ）：**LK-09 层级Ⅰ；HKJ-05 比萨斜塔自由落体层级Ⅰ；JK-10 枯井扔石子回声层级Ⅰ。**
- 任何触及题目数据的逻辑改动，先停下来问仓库所有者，不要自行决定。

## 代码现状（改之前必读）
- 多代叠加，有重复定义的函数：`generateVariation` 出现三次、`resourceCard` 两次，CSS 里混着 v6 / v13 / v14 / v23 残留。生效的通常是最后一个 `window.xxx=` 赋值或最后一个函数声明。重构去重时务必在浏览器里实跑、对照页面行为确认生效版本，再删旧副本。
- 五个页面：home / library（原题）/ integrate（整合）/ variation（变式）/ research（研究）。
- 题目产物三态：学生练习版 / 教师备课版 / 课堂投屏版，已有复制按钮和 `@media print` 打印。
- "AI 成品题生成区"是离线的：生成结构化提示词 → 人工把 AI 结果粘回 → 拆成卡片。不联网、无内置 API key。

## 部署约束
- GitHub Pages 是纯静态托管，没有请求时后端，前端不能存 API key。
- 无论源码怎么重构，必须始终保留一个"可部署产物"（单文件或 `/docs` 目录），让教师用一个链接 / 一个文件就能用。

---

## 工作顺序（分阶段，每完成一段先让我确认再进下一段）

### 阶段 0 ｜工程基线（不改功能、不动数据）
1. 源码拆分：HTML / CSS / JS 分离；图片从 base64 抽到 `images/`。
2. 加一个构建步骤，产出单文件或 `/docs` 的可部署版本。
3. 删除重复定义的函数（先在浏览器实跑确认生效版本），做一遍回归，保证页面行为和现在一致。
4. 配 GitHub Actions：push 即自动构建并部署到 Pages。
> 本阶段交付物：行为完全不变、但可维护的代码 + 自动部署。一行功能都不新增。

### 阶段 1 ｜教师可用性
1. 原题页筛选改成教师的语言：课时 / 知识模块 / 难度 / 情境 / 物理模型，加关键词搜索（"刹车""斜面""超速"能命中）。难度在界面显示为"基础 / 进阶 / 挑战"，但底层数据仍是层级Ⅰ/Ⅱ/Ⅲ，不改数据。
2. 产物卡：教师备课版的完整标准答案前置且可验算；给每份产物加 `status` 字段，"成品（可直接上课）"与"草稿骨架（需教师精修）"在 UI 上用角标 / 底色 / 文案彻底区分，避免教师把骨架当成品发给学生。
3. 投屏版改为分步呈现：先只出题干，再依次展开"圈画已知 → 画模型图 → 列式 → 解答"，大字号、一屏一题。
4. 讲义打印加固：题号 / 小问编号不触发浏览器自动列表导致错乱；数值与单位之间用不断行空格，避免 "5 m/s²" 断行。
5. 移动端 / 平板适配：找题、看答案、投屏在窄屏下可用。

### 阶段 2 ｜可选：真·AI 在线生成（仅在确实需要时）
- 在 Pages 之外挂一个 serverless 代理（Cloudflare Workers / Vercel / Netlify Functions），API key 作为后端 secret，前端调用代理而不是直连 Anthropic。
- 必须加访问控制（共享口令或调用频率限制），否则公开端点会被人刷爆 API 额度（费用算在仓库所有者头上）。
- 不需要真 AI 就跳过本阶段，现有离线提示词方案已够用。

---

## 提交规范
- 每阶段、每个独立改动单独提交；提交信息写清改了什么、**是否触及编码数据**（正常应为"未触及编码数据"）。
- 涉及题目数据或维度赋值的改动，一律先停下来问，不自行提交。
