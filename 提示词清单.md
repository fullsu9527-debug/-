# Claude Design 打磨提示词清单
多版本高中物理例题资源研究平台 · 整体打磨用

> 用法：上传本 ZIP 后，按 ①→②→③→④（六遍）的顺序复制提示词发送，⑤随时插用。
> 代码已在 index.html 里，无需手动粘贴；让它自己读文件即可。
> 别主动把整份 index.html 复制进对话框，省上下文。只有它表现得像没读到代码时才手动贴兜底。

---

## ① 开场（上传 ZIP 后第一条）

这是我的硕士论文配套网页"多版本高中物理例题资源研究平台"，纯 HTML+CSS+JS，95 道教材例题的截图在 images/ 文件夹里。它有六个页面：首页、原题库、文本分析（图表看板）、横向切片、整合决策工具、六维拓展工作台。

请你先完整通读 index.html，理解现有的页面结构、数据驱动逻辑和交互，然后用一段话向我复述你看到的整体信息架构，确认你读懂了。

重要约束：这是要用于学位答辩的研究展示工具，所有功能、数据和交互必须保留，不能删改任何 JS 逻辑或统计数字。我们要做的是视觉与体验的整体打磨，不是重做。这一轮你先不要改代码，只要确认理解。

---

## ② 定调（确认它读懂后）

现在定一下整体设计方向。这是一个面向物理教研和学术答辩的工具，气质应该是：专业、克制、有研究感，不要花哨的营销风。配色上目前用了坐标纸网格底纹和六版本固定识别色，认知层级Ⅲ用红色——这套语义编码请保留，因为它对应论文里的真实含义，不能为了好看乱改颜色含义。

请基于现有代码，给我 2-3 个首页的整体风格方向，每个方向说明它的排版逻辑、字体搭配和留白策略的差异，让我先选一个基调，再逐页铺开。先只做首页，其它页面不动。

---

## ③ 重构信息架构（选定基调后，逐页打磨之前）

请在逐页打磨视觉之前，先帮我重构首页和导航的信息架构。这一步只重排结构和首页，其它页面的功能逻辑和统计数字一律保持不变。

一、导航与页面顺序

把导航和页面顺序固定为这条论文主线，不要打乱：原题库 → 文本分析 → 横向切片 → 整合策略 → 拓展策略，对应"素材 → 发现 → 聚焦 → 对策一 → 对策二"。每页顶部加一句过渡语，页尾加一个"下一步 →"按钮引导顺序浏览（同时保留自由跳转）。各页过渡语如下：

- 原题库：一切结论的源头是这 95 道真实例题，每道都经过六个维度的精细编码。
- 文本分析：对 95 道题做横向统计，六个维度逐一比较，结构性差异随即浮现。
- 横向切片：宏观统计会不会只是数字巧合？选取交通制动这一最具代表性的模型逐题剖析，用微观证据印证宏观结论。
- 整合策略：既然单一版本各有缺口，第一条对策是跨版本调配现有例题——融合、补充、替换。
- 拓展策略：当六版都无可调资源，第二条对策是以原题为母题定向生成变式，补齐层级Ⅲ与工程情境的空白。

二、首页重构

首页要开门见山抛出研究结论，布局为"1 行指标条 + 1 个主视觉卡片 + 3 张次级卡片"，每张卡片可点击跳转到对应分析页面。

顶部定位语：本研究对六版必修一全部 95 道例题做六维全样本编码，发现例题资源在认知层级与情境配置上的结构性失衡，并据此提出整合与拓展策略。

指标条（一行小指标）：95 道全样本 · 6 个版本 · 6 个编码维度 · 全量精编非抽样。

主视觉卡片（最大，放最显眼处）：全样本层级Ⅲ仅占 10.5%（10/95），且 60% 集中在运动学，相互作用模块只有 1 道——高阶思维训练严重不足且分布失衡。点击跳转文本分析页。

三张次级卡片并列：

1. 相互作用与力模块中，工程技术情境的例题全六版加起来只有 1 道。点击跳转文本分析页。
2. 鲁科版 100% 案例闭环式，沪科版 100% 标准三段式，粤教版层级Ⅲ占 18.8% 却中间层仅 31.2%——版本差异不在数量而在设计取向。点击跳转横向切片页。
3. "交通制动安全"模型共 16 道，层级Ⅲ密度高达 31.3%，是全样本 10.5% 的三倍。点击跳转横向切片页。

完成后先给我看首页和导航效果，确认结构对了，再进入逐页视觉打磨。

---

## ④ 逐页打磨（结构改好后，六页各发一条）

通用模板（替换【页面名】和关注点）：

> 把刚才确定的设计语言应用到【页面名】这一页。重点看：【关注点】。功能和交互逻辑保持不变，只调视觉和布局。改完给我看效果。

各页关注点：

| 页面 | 关注点 |
|------|--------|
| 原题库 | 筛选工具栏的视觉层级、例题卡片的信息密度和可读性、95 道题长滚动下的节奏感。截图是这页核心，确保图片清晰不变形，模态框查看原图体验要好 |
| 文本分析 | 六张图表的可读性、配色对比度、悬停交互的反馈、点击图表回跳例题的引导 |
| 横向切片 | 版本×层级矩阵的视觉清晰度、芯片的可点击感、切片例题列表的层次 |
| 整合策略 | 左中右三列工作台的视觉平衡、候选题徽章的辨识度、整合草案排版的可读性 |
| 拓展策略 | 母题卡可编辑面板的表单体验、变式输出的排版层次、改造前后对比表的清晰度 |
| 首页 | 回看整体一致性，与其它五页风格是否统一，指标条和卡片的间距、字号层级是否到位 |

---

## ⑤ 随时可插的修正句

它想重写逻辑时：
> 停一下，不要重写这部分逻辑，只改它的 CSS 样式和布局，JS 行为保持原样。

它改坏了功能时：
> 刚才的改动让【某功能】不工作了，请只回退这一处，保留其它视觉改动。

要它先自检不动手：
> 帮我审一下这页的对比度、信息层级和移动端适配，列出还能优化的点，但先别动手。

要风格变体：
> 这个卡片样式给我 2-3 个变体对比一下，我选一个。

保护语义色：
> 提醒：六版本识别色和层级Ⅲ的红色是论文语义编码，不能改含义，只能调明度饱和度等视觉细节。

要移动端适配：
> 请专门为这一页做手机和平板适配，95 题矩阵和三列工作台在窄屏下要能正常使用，不要塌版或溢出。

---

## 注意事项

- 逐页改、逐页验收，是防止大改弄坏功能的关键。
- 迁移时图片是相对路径 images/img-XXX.png，务必连 images 文件夹一起带走。
- localStorage（OCR 校对文本、要素编辑）在不同环境不互通，属正常现象。
- 打磨完它导出的仍是 HTML/CSS，可再合回完整版。
